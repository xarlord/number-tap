<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Only back up SharedPreferences (high score, theme, sound prefs) -->
    <include domain="sharedpref" path="." />
    <!-- Exclude everything else by not including it -->
</full-backup-content>
