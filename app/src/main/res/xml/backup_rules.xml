<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude shared preferences containing user game data -->
    <exclude domain="sharedpref" path="."/>
    <!-- Exclude any cache files -->
    <exclude domain="file" path="cache/"/>
</full-backup-content>
